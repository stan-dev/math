# Generated by `boostdep --cmake compat`
# Copyright 2020, 2021 Peter Dimov
# Distributed under the Boost Software License, Version 1.0.
# https://www.boost.org/LICENSE_1_0.txt

cmake_minimum_required(VERSION 3.8...3.20)

project(boost_compat VERSION "${BOOST_SUPERPROJECT_VERSION}" LANGUAGES CXX)

add_library(boost_compat INTERFACE)
add_library(Boost::compat ALIAS boost_compat)

target_include_directories(boost_compat INTERFACE include)

target_link_libraries(boost_compat
  INTERFACE
    Boost::assert
    Boost::config
    Boost::throw_exception
)

target_compile_features(boost_compat INTERFACE cxx_std_11)

if(BUILD_TESTING AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/test/CMakeLists.txt")

  add_subdirectory(test)

endif()
